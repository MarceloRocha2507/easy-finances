

## Plano

Alterar o estado inicial de `collapsed` de `false` para `true` em `src/components/dashboard/AlertasInteligentes.tsx` (linha 54).

```typescript
// De:
const [collapsed, setCollapsed] = useState(false);
// Para:
const [collapsed, setCollapsed] = useState(true);
```

